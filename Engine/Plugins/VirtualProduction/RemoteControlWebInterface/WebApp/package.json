{
  "name": "conductor",
  "version": "0.1.6",
  "scripts": {
    "postinstall": "concurrently \"pushd Client && npm install && popd\" \"pushd Server && npm install && popd\"",
    "build": "node ./Scripts/build.js",
    "dev": "concurrently \"pushd Client && npm start && popd\" \"pushd Server && npm start && popd\"",
    "start": "pushd Server && npm run serve && popd"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.3.0",
    "fs-extra": "^9.0.1"
  }
}
