<UserControl x:Class="Timing_Data_Investigator.Controls.TimingDataTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Timing_Data_Investigator.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ContextMenu  x:Key="RowMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
            <MenuItem Header="{Binding Name, StringFormat=Open {0} in New Tab}" Command="{Binding OpenCommand}" />
        </ContextMenu>
        <Style x:Key="FilesRowStyle" TargetType="{x:Type DataGridRow}">
            <Setter Property="ContextMenu" Value="{StaticResource RowMenu}" />
        </Style>
    </UserControl.Resources>

    <TabControl x:Name="SummaryTabs">
        <TabItem x:Name="FilesTab" Header="Files" Visibility="Collapsed">
            <local:TimingDataGrid x:Name="FilesGrid" DockPanel.Dock="Top" />
        </TabItem>
        <TabItem x:Name="IncludesTab" Header="Includes">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				<StackPanel Grid.Row="0" x:Name="FlattenIncludesRow" Orientation="Horizontal">
					<TextBlock Text="Flatten Includes:" VerticalAlignment="Center" Margin="4 2" />
					<CheckBox x:Name="FlattenIncludes" Checked="FlattenIncludes_Checked" Unchecked="FlattenIncludes_Checked" VerticalAlignment="Center" Margin="2"/>
				</StackPanel>

				<local:TimingDataGrid Grid.Row="1" x:Name="IncludesGrid" DockPanel.Dock="Top" />
			</Grid>
		</TabItem>
        <TabItem x:Name="ClassesTab" Header="Classes">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
				<StackPanel Grid.Row="0" x:Name="GroupClassesRow" Orientation="Horizontal">
					<TextBlock Text="Group Template Classes:" VerticalAlignment="Center" Margin="4 2" />
					<CheckBox x:Name="GroupClassTemplates" Checked="GroupTemplates_Checked" Unchecked="GroupTemplates_Checked" VerticalAlignment="Center" Margin="2" />
                </StackPanel>

                <local:TimingDataGrid Grid.Row="1" x:Name="ClassesGrid" />
            </Grid>
        </TabItem>
        <TabItem x:Name="FunctionsTab" Header="Functions">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" x:Name="GroupFunctionsRow" Orientation="Horizontal">
					<TextBlock Text="Group Template Classes:" VerticalAlignment="Center" Margin="4 2" />
					<CheckBox x:Name="GroupFunctionTemplates" Checked="GroupTemplates_Checked" Unchecked="GroupTemplates_Checked" VerticalAlignment="Center" Margin="2" />
                </StackPanel>

                <local:TimingDataGrid Grid.Row="1" x:Name="FunctionsGrid" />
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
